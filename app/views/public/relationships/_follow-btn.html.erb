フォロー数：<%= @customer.followings.count %>
フォロワー数：<%= @customer.followers.count %>
<% if customer != current_customer %>
  <% if current_customer.following?(customer) %>
    <%= link_to "フォロー解除", customer_relationships_path(customer.id), method: :delete, remote: true %>
  <% else %>
    <%= link_to "フォローする", customer_relationships_path(customer.id), method: :post, remote: true %>
  <% end %>
  <% if (current_customer.following? customer) && (customer.following? current_customer)  %>
    <%= link_to 'チャットを始める', chat_path(customer.id)%>
  <% end %>
<% end %>