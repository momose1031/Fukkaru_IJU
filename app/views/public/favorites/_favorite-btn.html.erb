<% if customer_signed_in? %>
  <% if post.favorited_by?(current_customer) %>
    <p>
      <%= link_to post_favorites_path(post), remote: true, method: :delete do %>
        ♥<%= post.favorites.count %>
      <% end %>
    </p>
  <% else %>
    <p>
      <%= link_to post_favorites_path(post), remote: true, method: :post do %>
        ♡<%= post.favorites.count %>
      <% end %>
    </p>
  <% end %>
<% end %>